# Contributing

Use Python 3.11+, create a branch, run `pip install -e '.[dev]'`, then run `ruff check .` and `pytest` before opening a pull request. Use only synthetic or properly aggregated data. Do not commit credentials, PHI, secrets, or real vulnerability details for identifiable facilities.
